#!/bin/sh
#set -x

# user check
if [ "x`whoami`" != "xspace" ]; then
	echo "This script can be run only by space account"
	exit 4
fi

case "$1" in
	freebsd)
		echo "Setting Makefile for FreeBSD....."

		/bin/rm /space/space/src/libs/key/Makefile
		/bin/rm /space/space/src/apps/archspace/Makefile

		/bin/ln -s /space/space/src/libs/key/Makefile.FreeBSD /space/space/src/libs/key/Makefile
		/bin/ln -s /space/space/src/apps/archspace/Makefile.FreeBSD /space/space/src/apps/archspace/Makefile

		echo "The job has finished"
	;;

	solaris)
		echo "Setting Makefile for Solaris....."

		/bin/rm /space/space/src/libs/key/Makefile
		/bin/rm /space/space/src/apps/archspace/Makefile

		/bin/ln -s /space/space/src/libs/key/Makefile.Solaris /space/space/src/libs/key/Makefile
		/bin/ln -s /space/space/src/apps/archspace/Makefile.Solaris /space/space/src/apps/archspace/Makefile

		echo "The job has finished"
	;;

	*)
		echo "Unknown platform symbol!"
	;;
esac

exit 

