<?
  Header("Content-type: image/gif");
  Header("Pragma: no-cache");

  if(!$ctxt) $ctxt = "AdInfoS";
  if(!$cval) $cval = 0;

  if($ctxt != "AdInfoS" && $ctxt != "AdInfoP")
	$ctxt = "AdInfoS";

  if($ctxt == "AdInfoP") {
    $a = getdate(time() + 24*3600);
    $expire = mktime(0, 0, 0, $a[mon], $a[mday], $a[year]);
  }
  else {
    $expire = ""; 
  }

  setcookie($ctxt, $cval, $expire, "/", ".archspace.co.kr");
  setcookie($ctxt, $cval, $expire, "/", ".archmage.co.kr");

  echo ("GIF89a\001\000\001\000\200\000\000\000\000\000\000\000\000\041\371\004\001\000\000\000\000\054\000\000\000\000\001\000\001\000\100\002\002\104\001\000\073");
?>
